<!DOCTYPE html>  
<html>  
<head>  
    <title>Messages</title>
    <link rel="stylesheet" href="style.css">
</head>  
<body>  
    <h1>Messages</h1>  
    <ul>  
        <% usernames.forEach((username) => { %>  
            <li>  
                <strong><%= username.username %>:</strong> <%= username.text %> Timestamp: <em>(<%= username.date %>)</em>
                <form action="/delete" method="POST" class = 'delete-form' onsubmit="return confirm('Are you sure you want to delete this message?');">
                    <input type="hidden" name="username" value="<%= username.username %>">
                    <input type="hidden" name="text" value="<%= username.text %>">
                    <button type="submit">Delete</button>
                </form>  
            </li>  
        
        <% }) %>  
    </ul>  
    <a href="/new">New Message</a>  
</body>  
</html>